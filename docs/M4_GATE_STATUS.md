# M4 ゲート状況

最終更新: 2026-09-08（JST）

M4は「コードがある」だけで完了とはせず、自動検査、初見プレイ、独立レビューを分けて判定する。2026-08-29のiPhone実機初見プレイで、ゲームの目的・操作・結果が理解できない不合格を確認した。P0の画面構成とThree.js移行はmainへ反映済みだが、iPhone実機と初見プレイの再確認が終わっていないため、M5（ステージ4〜6）の制作は開始しない。

## 現在の判定

| 項目 | 状態 | 根拠 |
| --- | --- | --- |
| タイトルからゲーム開始 | 要改善 | ステージ選択と開始は動くが、実機初見でゲームの目的と最初の操作が伝わらなかった |
| 手番から結果表示 | 要改善 | trace再生と結果判定は動くが、実機初見で雨・水流・結果の因果が伝わらなかった |
| 保存・再開・破損復旧 | 完了 | StageSaveV1、hash照合、定義版照合、解放状態照合 |
| タイマー・一時停止・復帰 | 自動検査済み | Page Visibility、再生中復帰、古いcallback、二重開始を検査 |
| 狭い画面の操作 | P0対応中 | 操作シート、固定操作バー、候補選択と盤面配置の分離を追加。iPhone実機再確認待ち |
| 盤面上の縦スクロール | 自動検査済み／実機再確認待ち | ゲーム画面を盤面主画面とし、操作シート内へ操作を分離。Pointer中断で仮置きを解除しない |
| 盤面の意味・代替操作 | P0対応中 | 空の池、重要セルの座標、回転カメラ、補助情報の折りたたみ、候補カードの基準セル表示を追加。実機で視認性を再確認する |
| 水流再生の視認性 | 部分対応／実機再確認待ち | 水面を地形の後ろへ隠さない描画順と池の常時表示、回転対応、漏水セル強調を追加 |
| 読み上げ可能な状態説明 | 自動検査済み | 目的・雨予報をpolite live regionで通知 |
| 型検査・単体テスト・build・smoke | 合格 | 42 files / 217 tests、domain/app smoke。Three.js遅延チャンクと旧Canvas参照の静的検査を含む |
| 公開版のデスクトップ通し確認 | 部分確認 | 2026-09-05にmain反映後の公開版でタイトル、名前入力、ステージ1開始、WebGL失敗時の表示、再生成、ステージ選択への復帰を確認。WebGLが無効な環境のため、盤面操作から結果までの通し確認は未実施 |
| 実ブラウザ自動E2E | 部分確認／WebGL盤面未確認 | 接続済みChromeで公開版のDOM、開始導線、失敗画面、再生成、単一Canvasを確認。ブラウザ環境がWebGLを無効化しており、Three.jsの盤面入力・再生は未確認 |
| 初見プレイテスト | 実施・不合格（1名） | 目的、最初の操作、発生結果を理解できないまま終了。修正後に再実施する |
| 公開前独立レビュー | 未実施 | 初見プレイテスト後に実施 |
| M5ステージ4〜6 | 未着手 | M4手動ゲート通過まで開始しない |

## M4完了に必要な残作業

1. P0画面構成を公開版へ反映する。
2. iPhone実機で、回転、操作シート、盤面の池・雨・水・漏水位置を確認する。
3. 同じビルドを使い、初見プレイヤー3名以上でプロトコルを再実施する。
4. 目的理解、最初の施工、雨と水流の視認、最初の漏水原因、誤確定、継続意欲、ステージ1初見クリアを記録する。
5. 可能な環境で実ブラウザ自動E2Eを導入し、WebGL盤面入力、座標操作、確定、再生、結果表示を検査する。
6. 基準を満たした後、Sol・Highの独立レビューを受ける。
7. ユーザー本人がM4完了を判断してからM5へ進む。

## 記録の扱い

参加者の氏名や個人を特定できる情報は保存しない。結果はプロトコルの表へ、日付、ビルド識別子、人数、合否、修正内容だけを追記する。実機差の確認は別ゲートとして扱い、開発テンポを優先して先に単一ブラウザで実施してよい。

## 2026-08-29 実機初見プレイの記録

- 環境: iPhone実機、公開版
- 人数: 1名
- 判定: 不合格
- 観察: 何をするゲームか、何をすればよいか、画面で何が起きたかを理解できないまま終了
- 対応: タイトルの目的説明、ステージ目標、手番ごとの「今すること」、操作語の説明、雨・水流・結果の手番記録を追加。続く視覚修正で地形の高さ、水塊、雲・雨筋、流路・粒子、結果バナーを追加。修正後の再テストは未実施

## 2026-08-31 P0画面構成対応

- 構成: `docs/IMPLEMENTATION_STRUCTURE.md`へ、実装層、表示段階、カメラ責任、実装組織、検査ゲートを整理
- 画面: スマートフォンの盤面主画面と画面下操作シートの土台を追加
- 盤面: 90度単位の4方向回転、回転後も一致するセル入力・予報・水流表示を追加
- 視認性: 空の池、重要セルだけの番号表示、水面の描画順、失敗時の漏水セル強調を追加
- 入力: ポインター中断で仮置きを自動解除しないよう変更
- 状態: 自動検査は通過。iPhone実機再検証と初見3名テストは未実施

## 2026-09-03 Three.js WebGL盤面移行

- 対象: `work/threejs-webgl-board` のDraft Pull Request（PR #37、後にmainへマージ）
- 変更: 盤面本体をCanvas 2DからThree.js WebGLへ置き換え。Domain、Application、ステージ定義、保存、再生、ランキング、画面文章は変更していない
- 自動検査: `npm run typecheck`、`npm run test`、`npm run build:app`、`npm run test:app`、`npm run check`を実行して合格
- 未確認: 320／390／402／430px幅、横画面、WebGL context lost/restored、iPhone実機、初見プレイは未確認
- 判定: Three.js移行の自動検査は合格だが、M4完了とは扱わない。iPhone実機再検証、初見プレイ3名、独立レビューを継続する

## 2026-09-05 マージ後の公開版ブラウザ確認

- 公開版: `https://chameleonjp-lab.github.io/nagashimasu/`
- main: `a6234a5ff4d35962506feb40e10e91fd8683ca87`
- Pages: `Deploy GitHub Pages` 成功（run `33768132098`）。CIも同じmainで成功（run `33768132119`）
- 確認環境: 接続済みChrome。参加者による初見プレイではない
- 確認内容: タイトル表示、名前入力、ステージ1開始、Three.js遅延チャンクの読み込み、WebGL初期化失敗時の「3D表示を開始できませんでした」、再生成、ステージ選択への復帰、Canvasが1個のままであること
- 確認結果: WebGL context作成失敗を画面内で処理し、白画面にはならなかった。再生成後も失敗画面を表示し、ステージ選択へ戻れた
- 制限: このChrome環境はWebGLを無効にしているため、Three.js盤面、水流、回転、盤面のRaycaster入力は確認できなかった。Three.jsはcontext作成失敗をコンソールへ出力したが、アプリは例外を画面状態へ変換した
- 判定: 公開反映と失敗時導線は確認済み。M4の盤面実描画、iPhone実機、初見プレイ、独立レビューは未確認

## 2026-09-05 PR #39 マージ後のmain反映確認

- main: `effa01284edb0d8a3cbe1f21068cdd6053097d17`
- PR: #39（プレビュー全固定ステップ化、A1座標、ルールヒント、候補カードの基準セル表示）
- `Deploy GitHub Pages`: 成功（run `33966501217`）
- `CI`: 成功（run `33966501226`）
- M4判定: 自動検査と公開反映は確認済み。ただしWebGL有効な盤面操作、iPhone実機、初見プレイ3名、独立レビューは未確認

## 2026-09-05 初回タイマー設定の調整

- 対象main: `4995eafc5dc728b0ac9ddf7a6be338cb4630c8ba`（PR #40マージ後）
- ステージ3の標準タイマー20秒は残したまま、新規進捗の初期設定を長め30秒へ変更した。
- 標準20秒と無制限はステージ選択画面から引き続き選択できる。
- 既存の進捗保存に記録されたタイマー設定は変更しない。
- ステージ定義、勝敗、スコア、リプレイ、保存形式の版は変更していない。

## 2026-09-05 WebGL失敗時の説明改善

- WebGL初期化失敗またはcontext lost時の状態画面に、施工・雨・4回の水流・結果の流れを追加した。
- 等高面では水が動かないこと、盤外の出口は地形を上げても閉じないことを、静的な8×8盤面例とともに説明する。
- 盤面例は表示専用であり、実ステージの水流や勝敗を再計算しない。通常時のThree.js表示、Domain、ステージ定義、保存形式は変更していない。
- この対応はWebGL有効な盤面の代替操作ではないため、iPhone実機、初見プレイ3名、独立レビュー、M4完了判定は未実施のままとする。

## 2026-09-05 ステージ2の安全な出口表示

- ステージ2の安全辺に「安全な出口」ラベルを追加し、ステージ目標の説明にも緑の辺との対応を明記した。
- 水流計算、排水判定、目的値、ステージ定義、保存形式は変更していない。
- WebGL有効な実機での文字サイズ・重なり・視認性は未確認のため、M4の実機確認は継続する。

## 2026-09-05 結果画面のスコア単位表示

- 結果画面に、安全・工事効率・流量制御の配点と、ステージごとの制御スコアの単位を追加した。
- ステージ1では、クリア条件の24と制御スコアの評価上限32が別であることを明記した。
- ステージ3では、クリア条件が雨イベント数、制御スコアが守れた雨の水量であることを明記した。
- スコア計算、クリア判定、ステージ定義、保存形式は変更していない。実機での文字量・視認性は未確認のため、M4の実機確認は継続する。

## 2026-09-05 初手の準備を明示

- 最初の手番で雨がまだ降らないステージでは、「雨の前の準備」であることと、最初の雨の手番を「今すること」に表示する。
- 候補選択、仮置き、施工確定、水流計算、ステージ定義、保存形式は変更していない。
- 文章量と視認性はiPhone実機で未確認のため、M4の実機確認は継続する。

## 2026-09-05 表示文言の分離

- ステージ目標、進捗名、手番状態の表示文言を `src/presentation/stage-copy.ts` へ分離した。
- 画面表示の責任を整理する変更であり、水流・勝敗・スコア・保存形式は変更していない。

## 2026-09-05 再生・結果表示文言の分離

- 再生中の説明、結果バナー、失敗理由、操作拒否理由を `src/presentation/game-copy.ts` へ分離した。
- 既存の表示文言をそのまま純粋関数へ移した保守変更であり、水流・勝敗・スコア・保存形式は変更していない。

## 2026-09-05 モバイル操作シートのフォーカス遷移

- スマートフォンの操作シートを開いたとき、状態に応じて選択候補、施工確定、再挑戦へフォーカスを移すようにした。
- シートを閉じたときは従来どおり「工事を選ぶ／操作を開く」ボタンへフォーカスを戻す。
- ルール、水流、勝敗、スコア、保存形式は変更していない。iPhone実機とVoiceOverでの確認は未実施。

## 2026-09-07 盤面準備完了後のフォーカス再評価

- 初回起動や保存からの再開で3D盤面の読み込み中に操作シートを開いた場合、盤面の準備完了後に候補・施工確定などのフォーカス先を再評価するようにした。
- WebGLの読み込み待ちで「盤面へ戻る」に残ったままになるコード上の経路を解消した。
- ルール、水流、勝敗、スコア、保存形式は変更していない。iPhone実機とVoiceOverでの確認は未実施。

## 2026-09-07 ステージ別の水量表示上限

- 水量の見た目の上限を固定24から、各ステージの初期水量と予定雨量の合計を基準にする方式へ変更した。
- 24を超える水量でも、表示上の高さが同じにならないようにした。画面に表示する実水量と、Domainの水流・勝敗判定は変更していない。
- WebGL有効な実機での水面の見え方は未確認のため、M4の実機確認は継続する。

## 2026-09-07 モバイル操作シートのアクセシビリティ状態

- モバイル操作シートを開いている間はダイアログとして扱い、閉じている間は支援技術とキーボードフォーカスから除外するようにした。
- Escapeキー、Tabキーの循環、画面幅変更時の状態同期を追加した。
- ルール、水流、勝敗、スコア、保存形式は変更していない。iPhone実機とVoiceOverでの確認は未実施。

## 2026-09-07 初期画面テンプレートの分離

- 初期画面とゲーム画面のHTML組み立てを `src/presentation/app-markup.ts` へ分離し、`main.ts` は状態と画面進行を担当する形に整理した。
- DOMのID、操作シートのアクセシビリティ属性、既存の表示文言とリンクは変更していない。
- ルール、水流、勝敗、スコア、保存形式は変更していない。実機での画面表示は未確認。

## 2026-09-07 候補カード表示の分離

- 候補カードの形状、基準セル、向き、上げ下げ表示を `src/presentation/candidate-card.ts` の表示モデルへ分離した。
- `main.ts` は表示モデルをDOMへ反映し、候補カードの意味づけを純粋な単体検査で確認できるようにした。
- 候補の選択、施工、回転、ルール、水流、勝敗、スコア、保存形式は変更していない。iPhone実機とVoiceOverでの表示確認は未実施。

## 2026-09-07 狭い画面の開始前情報整理

- 520px以下では、目的・ステージ目標・最初の1手・名前入力・開始操作を残し、重複するゲームの流れ図と開始前のシェア・実験場リンクを非表示にした。
- デスクトップ表示、ゲーム中の操作、ルール、水流、勝敗、スコア、保存形式は変更していない。
- iPhone実機、文字拡大、VoiceOverでの表示確認は未実施。

## 2026-09-07 候補カードDOM表示の分離

- 候補カードのDOM生成を `src/presentation/candidate-card-dom.ts` へ分離し、`main.ts` に残っていた表示処理を整理した。
- 候補カードの形状、基準セル、向き、アクセシブルな説明は従来どおり維持した。
- 候補の選択、施工、回転、ルール、水流、勝敗、スコア、保存形式は変更していない。実機確認は未実施。

## 2026-09-07 座標ピッカー表示の分離

- 座標ピッカーのヘルプ、合法セル、選択状態、操作ロック中のアクセシブルな説明を `src/presentation/cell-picker-dom.ts` へ分離した。
- 操作ロック中の合法セルは、支援技術へ「現在は操作不可」と伝えるようにした。入力受理やルール判定は変更していない。
- iPhone実機、VoiceOver、WebGL有効環境での確認は未実施。

## 2026-09-08 PR1 クリア記録の保存順序

- main `8d129c8f7fb377ac78ccbe7628e393e51eae85f4` を基点に、受理済みクリアの進捗を演出開始前に保存し、保存成功後に対応する途中保存を片付けるApplication境界を追加した。
- 保存拒否・例外では現在の結果をページ内に保持し、未保存状態と再試行操作を表示する。演出の完了通知は進捗保存を行わない。
- 同じステージを再挑戦した後の途中保存を古いクリアの再保存で消さないよう、セッション・演出の無効化と保存対象の一致確認を追加した。水流・勝敗・スコア・Undo・保存形式・キーは変更していない。
- 型検査、単体検査（44ファイル／225件）、ドメインbuild・smoke、画面build・smokeは合格。WebGL有効経路、iPhone Safari、VoiceOver、初見3名、独立レビューは未確認であり、M4完了とは扱わない。

## 2026-09-08 PR2 水流の各段階と水量表示

- PR1マージ後のmain `d8b7c6ab6b29b952a77942fdf61eac6dc7f3d158` を基点に、`StageTraceEvent` の移動量・排水量から雨後と4回の水流後の表示用盤面列を作るApplication関数を追加した。
- `main.ts` は再生イベントごとの盤面を選び、`three-board-frame.ts` は水流中にプレビューの最終水量を混ぜない。目標進捗・雨予報・失敗強調も再生中の時点に合わせる。
- ステージ2のB4→C4→D4→E4→F4の水量、セル排水、盤外危険流出、台帳、Undo、再生成・保存再開後のハッシュを単体検査した。
- `npm run check` は45ファイル／229テスト、ドメインbuild・smoke、画面build・smokeを含めて合格。WebGL有効経路、iPhone Safari、横画面、文字拡大、VoiceOver、初見3名、独立レビューは未確認で、M4完了とは扱わない。

## 2026-09-09 PR3 排水口と水面の意味

- PR2マージ後のmain `481efd0a20c3ed30b74c1cba819591b4ae8c8a17` を基点に、排水能力を盤面へ渡し、セル内排水口を黄色い輪・内部記号・「排水口／最大x/回」で常設表示するようにした。
- `FlowStepResult.drains`の実排水量を「排水 −x」として水流再生・プレビューへ表示し、盤外の安全出口とは辺マーカーとラベルで区別した。
- `地形×8＋水量`を表示用の共通水面基準へ変換し、同じ地形ではなく同じ水面で流れが止まることを凡例と3D表示へ反映した。Domainの水流・排水・勝敗・スコア・保存形式は変更していない。
- `npm run check` は45ファイル／233テスト、ドメインbuild・smoke、画面build・smokeを含めて合格。iPhone 17 Pro、縦横幅、文字拡大、VoiceOver、初見3名、Sol Highの独立レビューは未確認で、M4完了とは扱わない。

詳細な確認手順は `docs/PR3_DRAIN_OUTLET_WATER_MEANING.md` に記録した。
